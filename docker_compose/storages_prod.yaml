services:
  postgres:
    image: postgres:15
    container_name: example-db
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ../.env
    networks:
      - hrresumebank-network
